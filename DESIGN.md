# Forcetris 설계 문서 — The Forge Map

이 문서는 이 게임의 헌법이다. 무엇을 만들지 헷갈릴 때, 밸런스를 만질 때,
새 기능이 어울리는지 판단할 때 여기로 돌아온다. 코드가 어떻게 돌아가는지는
`cpp/README.md`가 말하고, 이 문서는 **왜** 그렇게 만드는지를 말한다.

## 1. 정체성 — 한 줄

> **테트리스를 잘 몰라도 되는, 분기 맵 로그라이트 대장간.**

- 멀티플레이어 테트리스는 이미 고여 있고, 순수 싱글은 식상하다. 그래서
  싱글플레이어에 로그라이트의 뼈대(경로 선택, 런 빌드, 프레스티지)를 붙인다.
- **The Forge Map(캠페인)이 본편이다.** Ignition / Meltdown / Duel 등 일반
  모드는 수련장이고, 드래프트는 캠페인의 노드 보상으로 일원화됐다
  (V2.2a). 수련장도 Flow·오버드라이브는 그대로 돈다 — 보상 축은 게임
  전체의 것이다 (V2.2b).
- 테마는 대장간 하나로 통일한다: 퓨즈(심지), 히트(가열), 템퍼(담금질),
  엠버(불씨), 슬래그(광재), 모루. 새 시스템의 이름은 반드시 이 어휘장
  안에서 짓는다.

## 2. 목표 청중 — 완전 입문자·캐주얼

폰을 세로로 쥔, 테트리스를 "회전과 줄 지우기" 정도로만 아는 사람.
이 결정에서 기계적으로 따라 나오는 규칙들:

| 규칙 | 이유 |
|---|---|
| 핸들링 기본값은 TETR.IO 기본(167/33/17/6) | 모두가 배우는 게임의 감각에서 시작하고, 실력이 늘면 스스로 낮춘다 |
| 튜토리얼 화면은 만들지 않는다 | 노드가 곧 튜토리얼 — 새 규칙은 **노드당 정확히 하나**, 문구는 한 줄 |
| 챕터 1은 지는 게 더 어렵게 | 첫 세션의 임무는 가르치기가 아니라 붙잡기다 |
| 기본 난이도는 순한 불(아래 §4) | 죽음의 무게는 선택한 사람만 짊어진다 |
| 폰 세로 화면이 1급 시민 | 모든 새 화면은 세로 스크린샷 검증을 통과해야 출시 |
| 세션 5분이 완결된 재미 | 노드 하나 = 한 세션이 되도록 노드를 짧게 자른다 |
| **강제 드롭은 기본이 아니라 기믹** | 초보 병목 1호였다 (V2.1 게임감 아크). V2.2a에서 전역 완결: 게임 어디서든 기본은 순수 보드고, 심지는 캠페인의 번 룸(c1s7·c2s4·c3s7)과 캠페인 듀얼에서만 탄다 — 어둠처럼 그 방의 사건. 설정에도 룰 노브가 없다(스핀·클리어·킥·퓨즈 전부 게임의 소유) |

## 3. 플레이어의 목표 — 4계층

| 계층 | 목표 | 시간 단위 |
|---|---|---|
| 순간 | 이 조각을 가장 값진 곳에 놓는다 — 불붙은 방에서는 심지가 타기 전에 | 초 |
| 노드 | 이 노드의 사건을 읽고 별 셋을 딴다 | 분 |
| 런 | 갈림길에서 경로를 고르고, 빌드를 세워 보스까지 오른다 | 십 분 |
| 장기 | 챕터 격파 → 전 챕터 백열 클리어 → 엔드리스 기록 | 주 |

계층마다 보상이 있어야 한다: 순간은 연출(스파크·화면), 노드는 별과 엠버,
런은 슬래그와 격파 기록, 장기는 모루와 기록판.

## 4. 실패의 의미 — 프레스티지 + 난이도 선택제

**어느 난이도든 죽어도 슬래그는 남는다.** 남은 엠버가 슬래그로 렌더링되어
(prestige) 실패한 런도 모루 한 칸에 다가간다. 좌절이 아니라 재시도 유도.

죽음의 *무게*는 난이도가 정한다. 별도의 하드코어 모드를 만들지 않고
난이도 축 하나로 흡수한다:

| 난이도 | id | 죽으면 | 슬래그 배율 |
|---|---|---|---|
| 순한 불 (기본) | `mild` | 그 노드만 다시 — 템퍼·엠버·경로 유지 | ×1.0 |
| 벼린 불 | `forged` | 목숨 1 소모(맵당 3, 모루로 +1) — 다하면 런 리셋 | ×1.5 |
| 백열 | `white` | 런 통째 리셋 — 템퍼·엠버·경로 소멸 | ×2.0 |

- **재시작(R)도 같은 비용을 문다** (V2.1): 전투 중 R는 항복이다 —
  forged는 목숨 1, white는 런 종료. mild만 무료 재도전. 패배 정산을
  이미 치른 뒤의 재도전은 추가로 물리지 않는다.

- **난이도는 죽음의 무게만이 아니라 적의 등급도 정한다** (V2.7에서 재조정):
  벼린 불이 레시피에 적힌 등급 그대로이고 백열은 한 칸 위, **순한 불은 두 칸
  아래에 상한 B**(`campaign::kMildCeiling`). 예전엔 한 칸만 내렸는데, SS까지
  오르는 사다리에서 한 칸 내려봐야 길의 끝이 S였다 — 배우는 중인 사람이
  이길 수 있는 상대가 아니다. 이 게임의 대상은 §2가 못박은 대로 완전
  입문자이고, 입문자의 실력은 대략 D~C다. 순한 불의 모든 듀얼은 이제
  **E~B 구간**에 있고 길의 마지막 보스가 B다.
- **사다리에 리그 아래 두 칸(F·E)이 있다**: D는 TL 실제 평균이고, TL 실제
  평균은 이미 "테트리스를 하는 사람"이라 이번 주에 게임을 처음 만진 사람보다
  빠르다. F(0.45 PPS)·E(0.58)가 그 아래를 채운다 — 손이 느린 것이 핵심이다.
  블런더는 봇을 2등 수(手)로 내릴 뿐이고 2등 수도 잘 쌓기 때문에, 초보가
  한 수 생각할 시간을 주는 레버는 속도뿐이다.
- 난이도는 런 시작 시 고르고 런 중에는 못 바꾼다.
- 배율 숫자는 잠정. 원칙만 고정: 무거운 죽음일수록 프레스티지가 빠르다.

## 5. 런의 해부 — 분기 맵

Slay the Spire 문법. 챕터 하나 = 시드로 생성되는 분기 그래프 = 런 하나.

```
        [보스]
       /      \
  [전투]      [미니보스]
     |    ×      |
  [대장간]    [이벤트]
       \      /
   [전투]  [전투]      ← 깊이 6 (갈림길 2~3개)
        \  /
       [전투]
        (입구)
```

- **깊이 6**: 전투 3~4 + 이벤트/대장간 1~2 + (미니보스) → 보스.
  8스테이지 일렬은 캐주얼에게 호흡이 길다 — 한 런 10~15분을 지킨다.
- **노드 종류**
  - `battle` 전투: 스테이지 레시피 하나 (기존 16 레시피가 풀).
  - `forge` 대장간: 드래프트(3장 중 1) + 엠버 상점(리롤, 추가픽, 나중엔 카드 제거).
  - `event` 이벤트: 짧은 선택지 한 장 — 위험/보상 거래 (예: "심지를 잘라
    엠버를 얻는다" = fuse_scale 페널티 + 엠버).
  - `rest` 휴식: 다음 전투 하나에 퓨즈 보너스, 또는 템퍼 하나 강화.
  - `miniboss` / `boss`: 듀얼 (봇 + 블레이드).
- **전투 후 보상**: 카드 3장 중 1 + 엠버. 스테이지 **도중** 드래프트는
  캠페인에서 없다 — 보드는 절대 멈추지 않는다. 히트(10줄)는 퓨즈
  타이트닝만 담당한다.
- **런 상태**: 템퍼 스택·엠버·경로·목숨이 맵 한 바퀴 동안 유지된다.
  빌드가 노드 여섯 개에 걸쳐 자라는 것이 이 게임의 로그라이트다.
- **갈림길의 문법**: 항상 "안전한 길 vs 위험하지만 보상 큰 길"이 읽히게.
  노드 아이콘과 한 줄 미리보기로 선택지를 설명 없이 전달한다.
- **수문장 쌍 로테이션 (V2.3)**: 챕터마다 **컨셉 쌍 3조**(미니보스 1 +
  보스 1)가 대기하고, 런은 시드가 굴린 **한 조만** 만난다. 세 조의 랭크는
  V2.2c 사다리로 동일하다 — 어느 얼굴이 떠도 챕터의 난이도는 같고, 달라지는
  것은 **싸움의 모양**이다: 수문장(기존)은 랭크 기본 블레이드에 스킬 1/2,
  망치는 **한 등급 위 블레이드에 스킬 없음**(순수 체급), 재간꾼은 **한 등급
  아래 블레이드에 스킬 2/3**(약하지만 계속 훼방). 정체는 **한 줄 전에**
  드러난다: 무엇이 있는지(보스/미니보스)는 아이콘으로 처음부터 보이지만
  이름과 블러브는 그 줄 바로 아래에 설 때 `? ? ?`에서 풀린다. 테이블은 더
  이상 위치로 읽히지 않는다 — `Stage::role`(방/미니보스/보스)과
  `Stage::pair`가 있고, 배틀 윈도는 "role 0 집합"이므로 듀얼 레시피를
  아무리 더해도 기존 시드의 맵 추첨은 그대로다(쌍 추첨은 별도 난수
  스트림). 챕터 게이트와 엔드리스 해금은 "직전 챕터의 **아무** 보스"를
  읽는다.
- **엔드리스 등반 The Endless Climb (V2.2)**: 챕터 2 보스를 꺾으면
  열리는 무한 모드. 같은 6줄 맵이 **링**으로 무한히 쌓인다 — 배틀은 전
  챕터 풀에서 추첨(윈도가 링마다 위로 슬라이드, 풀 꼭대기에 닿으면 최고
  난이도 근방에서 계속), 꼭대기는 **수문장 로테이션**(링 0~5:
  c1m1→c1s8→c2m1→c2s8→c3m1→c3s9, 이후 Vault Warden↔Forge Heart 교대).
  링이 오를수록 조여진다: 중력·쿼터·점수 목표·생존 시간 증가, 듀얼 봇은
  링당 반 랭크 승급(상한 7). 난이도는 항상 백열(한 번 죽으면 끝, 슬래그
  ×200%)이고 기록은 **오른 행 수**(ring×6+depth) — 기록판은
  `endless_best` 한 줄이다. 생성기는 `build_endless_map(ring, seed)`로
  캠페인 맵과 같은 약속(형태·연결·비교차)을 campaigncheck에서 같이
  검사받는다.

## 6. 기믹 사전 — 이벤트형이 기본

원칙: **입문자의 눈에 보이는 사건**이어야 한다. "킥 없음"은 규칙서고,
"용암이 차오른다"는 사건이다. 파라미터 트윅은 사건의 재료로만 쓴다.

구현 비용별 분류 (로드맵의 근거):

| 기믹 | 사건 | 보이는가 | 구현 |
|---|---|---|---|
| 잔해 Rubble | 바닥에 고철이 깔려 있다 | ◎ | 있음 (`board` 프리셋) |
| 용암 상승 Rising Slag | 바닥이 주기적으로 차오른다 + 흔들림·굉음 (V2.0-c) | ◎ | 있음 (`cheese_period` + 상승 연출) |
| 압박 Backdraft | 심지가 온 스테이지 내내 벌겋다 | ○ (연출 보강) | 있음 (`pressure`) |
| 어둠 Dim | 등불 반경 밖은 어둠, 등불이 조각을 따라 미끄러짐 | ◎ | **구현 (V2.0-c, c1s9)** |
| 안개 Fog | NEXT 큐가 첫 조각 빼고 연기에 가림 | ◎ | **구현 (V2.0-c, c2s9)** |
| 열 잠금 Sealed Columns | 리벳 강판이 바깥 열을 막는다 — 좁아진 화덕 | ◎ | **구현 (V2.1, c1s5, sealed_check)** |
| 얼음 조각 Cold Iron | 완성된 줄이 얼어붙고, 다음 잠금에 깨진다 | ◎ | **구현 (V2.1, c2s7, cold_check)** |
| 심지 Burn | 이 방만 심지가 탄다 — 다 타면 강제 드롭 | ◎ | **구현 (V2.1, c1s7·c2s4·c3s7)** — V2.2a부터 전역 기본 없음, **V2.5부터 듀얼에서도 없음**. 번 룸 셋에만 남는다 |
| 점수 목표 | 줄이 아니라 점수가 목표 — 스핀·쿼드가 지름길 | ◎ | **구현 (V2.1, c1s10·c2s10)** |
| 거대/미니 조각 | 조각 크기가 변한다 | ◎ | 심 신규 + ctest (대형) |

- 심 신규 기믹은 반드시 전용 ctest와 함께 들어온다 (§10).
- 기믹은 노드당 하나 — 챕터 1·2에서. **챕터 3(V2.2)이 조합의 집**이다:
  어둠+열 잠금(c3s2), 점수+어둠(c3s3), 얼음+안개(c3s4), 얼음+잔해(c3s6),
  심지+압박(c3s7) — 전부 이미 각각 따로 가르친 것들의 겹침이다.

## 7. 현재 인벤토리 (51 레시피)와 재배치

id는 세이브에 박제되어 있으므로 **영원히 동결**. 숫자와 옷은 자유.
맵 시대에는 이 51개(챕터 1: 17, 챕터 2: 18, 챕터 3: 16)가 "인카운터
풀"이 된다 — 엔드리스 등반은 세 챕터의 **방(role 0)** 합집합에서 추첨한다.
듀얼 18장(챕터당 쌍 3조 × 2)은 풀 밖의 수문장이고, 런은 그중 한 조만
만난다.

| id | 이름 | 지금 기믹 | 판정 | 재배치 |
|---|---|---|---|---|
| c1s1 | First Sparks | 쿼터 10 | 보임 | 그대로 — 첫 전투 |
| c1s2 | The Scrap Pile | 잔해 3줄 | ◎ | 그대로 |
| c1s3 | The First Cut | 다운스택 | ◎ | 그대로 |
| c1s4 | Loose Mortar | 스티키 캐스케이드 | ○ | 무너짐 연출 보강 |
| c1s5 | Rusted Joints | **열 잠금** (킥 없음은 폐기) | ◎ | V2.1 재의상 완료 |
| c1s10 | Trial of Sparks | 점수 목표 7,000 | ◎ | V2.1 신규 |
| c1m1 | The Underwarden | 미니보스 듀얼 + 녹비(rustfall) 스킬 | ◎ | V2.1 신규 |
| c1k1 | Scrap Whelp | 잡몹 스커미시 (배틀 풀 내 약한 듀얼) | ◎ | V2.1c 신규 |
| c1s11 | The Long Watch | 시간 생존 75초 + 상승 바닥 | ◎ | V2.1c 신규 |
| c1s6 | The Rising Floor | 상승 | ◎ | 그대로 |
| c1s9 | The Dark Gallery | 어둠(등불) | ◎ | V2.0-c 신규, c1s6과 c1s7 사이 |
| c1s7 | Backdraft | **심지 기믹의 집** — 압박+짧은 심지 | ◎ | 캠페인에서 심지가 타는 두 방 중 하나 |
| c1s8 | The Gatekeeper | 보스 FT1 | ◎ | 챕터 1 보스 — 쌍 0(수문장) |
| c1m2 | The Slag Fist | 망치 미니보스 rank 2, 블레이드 4장·무스킬 | ◎ | V2.3 신규 — 쌍 1 |
| c1b2 | The Anvil-Breaker | 망치 보스 rank 3 FT1, 블레이드 5장·무스킬 | ◎ | V2.3 신규 — 쌍 1 |
| c1m3 | The Lamplighter | 재간꾼 미니보스 rank 2, 블레이드 2장 + 녹비·봉인 | ◎ | V2.3 신규 — 쌍 2 |
| c1b3 | The Rust Weaver | 재간꾼 보스 rank 3 FT1, 블레이드 3장 + 스킬 3종 | ◎ | V2.3 신규 — 쌍 2 |
| c2s1 | Heavier Air | 중력↑ (심지 리더 폐기) | ○ | 낙하 연출 보강 후보 |
| c2s10 | Weight in Gold | 점수 목표 16,000 + 빠른 낙하 | ◎ | V2.1 신규 |
| c2s2 | Three Cuts | 다운스택 홀3 | ◎ | 그대로 |
| c2s3 | Chain Collapse | 링크드 캐스케이드 | ○ | 연출 보강 |
| c2s9 | Smoke in the Rafters | 안개(NEXT 가림) | ◎ | V2.0-c 신규, c2s3과 c2s4 사이 |
| c2s4 | The Overheated Wing | **심지 기믹의 집** — 압박+overheat 선탑재 | ◎ | 캠페인에서 심지가 타는 두 방 중 하나 |
| c2s5 | The Buried Hall | 잔해 5줄 (킥 없음은 폐기) | ◎ | V2.1 재의상 완료 — 규칙서형 기믹 전멸 |
| c2s6 | The Flood | 빠른 상승 | ◎ | 그대로 |
| c2s7 | Cold Iron | **얼음 조각** (파라미터 극단은 폐기) | ◎ | V2.1 재건축 완료 |
| c2m1 | The Quenchguard | 미니보스 듀얼 + 냉기(coldsnap) 스킬 | ◎ | V2.1 신규 |
| c2k1 | Ash Hound | 잡몹 스커미시 | ◎ | V2.1c 신규 |
| c2s11 | The Flood Watch | 시간 생존 90초 + 빠른 상승 | ◎ | V2.1c 신규 |
| c2r1 | The Kennel | 레이드 — 잡몹 3연전, 1패면 끝 | ◎ | V2.1c 신규 |
| c2s8 | The Forgemaster | 보스 FT2+블레이드 | ◎ | 챕터 2 보스 — 쌍 0, 엔드리스 해금 열쇠 |
| c2m2 | The Drop Hammer | 망치 미니보스 rank 3, 블레이드 5장·무스킬 | ◎ | V2.3 신규 — 쌍 1 |
| c2b2 | The Deep Kiln | 망치 보스 rank 4 FT2, 블레이드 5장·무스킬 | ◎ | V2.3 신규 — 쌍 1 |
| c2m3 | The Frost Cantor | 재간꾼 미니보스 rank 3, 블레이드 3장 + 냉기·봉인 | ◎ | V2.3 신규 — 쌍 2 |
| c2b3 | The Quench Choir | 재간꾼 보스 rank 4 FT2, 블레이드 3장 + 스킬 3종 | ◎ | V2.3 신규 — 쌍 2 |
| c3s1 | White Heat Rising | 쿼터 20 + 빠른 낙하 | ◎ | V2.2 신규 — 백열의 입구 |
| c3s2 | The Narrow Dark | **어둠+열 잠금** (양끝 봉인) | ◎ | V2.2 신규 — 첫 조합 |
| c3k1 | Cinder Wolf | 잡몹 스커미시 rank 4 | ◎ | V2.2 신규 |
| c3s3 | Gold in the Dark | **점수 20,000 + 어둠** | ◎ | V2.2 신규 |
| c3s4 | The Frozen Gallery | **얼음 조각 + 안개** | ◎ | V2.2 신규 |
| c3s5 | The Last Flood | 시간 생존 105초 + 최속 상승 | ◎ | V2.2 신규 |
| c3s6 | Rubble and Frost | **얼음 조각 + 잔해 5줄** | ◎ | V2.2 신규 |
| c3r1 | The Pack | 레이드 — 잡몹 3연전 | ◎ | V2.2 신규 |
| c3s7 | Backdraft Vault | **심지+압박** (셋째 번 룸, 빠른 심지) | ◎ | V2.2 신규 |
| c3s8 | Chain of Embers | 링크드 캐스케이드 쿼터 18 | ○ | V2.2 신규 |
| c3m1 | The Vault Warden | 미니보스 rank 5 + 봉인(sealgate) 스킬 | ◎ | V2.2 신규 |
| c3s9 | The Forge Heart | 최종 보스 rank 5(SS) FT2 + 스킬 셋(냉기·열파·녹비) | ◎ | V2.2 신규 — 쌍 0(수문장) |
| c3m2 | The White Weight | 망치 미니보스 rank 4, 블레이드 5장·무스킬 | ◎ | V2.3 신규 — 쌍 1 |
| c3b2 | The Bellows Titan | 망치 보스 rank 5 FT2, 블레이드 **7장**·무스킬 | ◎ | V2.3 신규 — 쌍 1, 최대 체급 |
| c3m3 | The Ember Sophist | 재간꾼 미니보스 rank 4, 블레이드 3장 + 열파·녹비 | ◎ | V2.3 신규 — 쌍 2 |
| c3b3 | The Chorus of Coals | 재간꾼 보스 rank 5 FT2, 블레이드 5장 + 스킬 3종 | ◎ | V2.3 신규 — 쌍 2 |

- "판정"은 §6의 기준: 입문자 눈에 사건이 보이는가.
- 재배치는 V2.0-c에서 실행. 이 표가 그 작업지시서다.

## 8. 경제 — 세 화폐, 세 시간 단위

| 화폐 | 시간 단위 | 벌기 | 쓰기 |
|---|---|---|---|
| 엠버 Embers | 런 | 전투 보상 (줄×2 + 공격×3, 모루 보너스 %) + 이벤트 + 스킵 위로금 4 (**물가 곡선을 같이 탄다**) | 리롤 6, 추가픽 14, 대장간: 카드 제거 8·**복제 16**·**목숨 25**(벼린 불, 방문당 1), 맵 상시: **Hot Oil 10**(다음 전투 공격↑)·**Frost Oil 12**(다음 듀얼 상대 동결), 십일조(이벤트) — V2.1e에서 소비처 확장 |
| 슬래그 Slag | 영구 | 노드 클리어 바운티 + 별 보너스; 죽으면 남은 엠버/5 | 모루 영구 업그레이드 |
| 별 Stars | 영구 | 노드당 최대 3 (클리어 / 파 시간 / 셋째 별은 방을 읽는다: 불붙은 방=무강제드롭, 순수 방=파의 3/4) | 진행 게이트, 과시 |

- **모루(영구업)**: 7종 — wick/bank/bellows/sense/preheat +
  **lifeblood(벼린 불 목숨 +1)**, **warchest(런 시작 엠버)** (V2.1d).
  남은 확장 후보: 갈림길 미리보기, 상점 할인. 원칙: 모루는 **캠페인
  안에서만** 효력 — 일반 모드 점수표는 영원히 순수하다.
- **카드 풀 (V2.4에서 34종)**: FUEL 5 / FLOW 7 / RISK 7 / RULE 4 /
  CHAOS 5 / **WARD 6**. **V2.5에서 CHAOS 5장은 백열(kWhite) 전용**이 되었다 —
  `temper::offer(..., chaos)`의 기본값이 "안 나온다"라 화면이 깜빡하고 안
  물어보면 새지 않고, 백열에서도 한 손에 한 장까지다. 엔드리스는 언제나
  백열이므로 혼란이 상시 도는 유일한 구역이다. 같은 V2.5에서 **FUEL은
  심지가 아니라 게이지를 먹인다**: 파낸 잔해·얻어맞은 줄이 Flow를 채우고,
  게이지의 천장이 높아지고, 불이 꺼져도 일부가 남고, 타는 중에 클리어로
  다시 채워진다. **방벽 WARD(V2.4)**는 여섯 번째 가문이자 유일한
  차가운 색(강청)이다 — 아무것도 더 빨리 이기게 해주지 않고 전부 살아남게
  해준다: 무게추(중력 완화), 자유로운 손(홀드 잠금 해제), 바닥 청소(여덟
  번째 잔해 클리어마다 바닥 잔해 한 줄), 냉담(듀얼에서 날아오는 쇳물이
  얇아진다), 냉각수(상대 오버드라이브의 열압이 덜하다), 체(잔해 구멍이 한
  줄로 선다). 절반이 조건부라 무게는 FUEL(4)이 아니라 RISK와 같은 3 —
  실은 FUEL 쪽이 더 조건부다(51개 레시피 중 퓨즈 방·듀얼 11개에서만 산다).
  **방의 기믹을 끄는 카운터 카드는 없다** — 완화는 하되 취소는 안 한다.
  나머지 V2.4 8장: deep_bank·hard_quench(FUEL), draught(FLOW — 오버드라이브
  점화선을 낮춘다), glass_edge·hair_trigger·hollow_wick(RISK),
  linked_chain(RULE — 미사용이던 링크드 캐스케이드), sticky_tongs(CHAOS —
  네 번째 홀드가 먹히고 Flow가 그 대가를 낸다). 혼란 가문(V2.2b)은 전부 *이익과 손해를 한 줄에* 적는다 —
  wild_spins(끼인 조각은 뭐든 풀 스핀 ↔ 코너 규칙 T스핀은 폐기),
  ring_walls(양끝 벽이 포탈 ↔ 중력 가속), crossed_wires(공격 배율 +0.5 ↔
  좌우·회전 키가 바뀜), loose_ratchet(Flow 충전 +3 ↔ 세 번째 회전마다
  한 번 더 돔). 뽑힐 확률은 RULE급 최저이고, 봇은 가문 통째로 절대 안 집는다.
  키를 뒤트는 두 장은 심이 아니라 GUI 입력 경로에서 걸린다(심은 언제나
  정직한 키를 받는다 = 그레이딩 불변). V2.1d 6종은 "재미있는 효과"
  방향 — heavy_hand(공격 배율),
  loaded_dice(매 3번째 타격 2배 — 심은 주사위를 안 굴리므로 결정론적
  카운터), cold_forge(내 줄도 얼지만 공격 +75%), turning_rack(클리어마다
  홀드↔큐 교환), frostbrand(듀얼: 상대 클리어 동결 — "상대 필드에 클리어
  불가 라인"의 실현), hobnails(듀얼 시작 시 상대 바닥에 녹 2줄). 카드
  문면에 숫자를 쓰지 않는 원칙은 유지. collapse·cold_forge·turning_rack은
  봇이 절대 집지 않는다(플래너 전제 보호).
- **후반 물가 (V2.4)**: 엠버 상점가는 전 구간 고정이 아니라 **오른 행 수**
  (캠페인 `chapter × 6 + depth`, 엔드리스 `ring × 6 + depth`)마다 **+8%,
  상한 ×3**으로 오른다. 챕터 3 꼭대기 ≈ ×2.4, 엔드리스 링 4 부근에서 상한.
  수입 쪽은 스킵 위로금만 같은 곡선을 타고, 전투 보상은 그대로 둔다 —
  방이 길어지면서 이미 늘어나기 때문이다. 모루(슬래그)는 예전부터
  `cost_base × n`으로 오르므로 무수정. 계산은 `campaign::priced` 한 곳,
  화면에 찍히는 숫자와 실제로 빠져나가는 숫자가 갈라질 수 없다.
- **preheat(예열)**는 맵 시대엔 "런 출발 직후 무료 스포일 한 손"으로
  재해석되었다 (V2.0-b). 대장간 노드가 생기면(V2.0-c) 그쪽으로 옮길지
  재평가한다.

## 9. 로드맵

종착지: **완성작 + 소규모 배포** (itch.io / APK 직배포, 스토어 심사 없음).

| 버전 | 내용 | 재미의 증가분 |
|---|---|---|
| **V2.0-a (완료)** | 이 문서 + 콘텐츠/메커니즘 분리 리팩토링 (stages.cpp, 챕터 테이블) | 없음 — 기반 공사 |
| **V2.0-b (완료)** | 런/맵 코어: 시드 분기 맵 생성 + 맵 화면 + 런 상태 저장 + 노드 보상 드래프트(캠페인 내 히트 드래프트 제거) + 난이도 3단 | 경로 선택과 런 빌드 — 게임의 새 심장 |
| **V2.0-c (완료)** | 이벤트형 기믹 1차(어둠·안개 신규 스테이지, 상승 연출) + 대장간 노드(무료 드래프트·리롤·추가픽·카드 제거 8) + 이벤트 노드(즉효 4종). 휴식 노드는 보류(지속 버프 저장 — V2.1로), 지진·인카운터 재의상은 V2.1의 심 기믹과 묶어서 | 노드마다 다른 사건 |
| **V2.1a (완료)** | 심 신규 기믹(열 잠금 c1s5, 얼음 조각 c2s7 — sealed_check/cold_check) + 미니보스 노드(c1m1/c2m1, kind 4) | 보이는 기믹 둘과 위험 갈림길 |
| **V2.1b 게임감 (완료)** | 강제 드롭 기믹화(캠페인 기본 심지 오프, c1s7·c2s4·듀얼만 온) + R=항복 비용 + 잠금 펄스·착지 흔들림 juice + 점수 목표 스테이지(c1s10/c2s10) | 초보 병목 제거, 조작감 |
| **V2.1c 전투 (완료)** | 대칭 듀얼 레이아웃(두 보드 같은 크기 좌우, 폰 세로는 상대 보드 확대) + 보스 특수 스킬(2초 텔레그래프: Gatekeeper 녹비+열 봉인, Underwarden 녹비, Quenchguard 냉기, Forgemaster 냉기+열파) + 잡몹 스커미시(c1k1/c2k1, 배틀 풀 내 약한 듀얼) + 레이드(c2r1 켄넬: 3연전, 1패면 끝) + 시간 생존(c1s11/c2s11 워치: 버티면 승리, 별은 클리어 라인 바) | 전투 다양성 |
| **V2.1d 경제 (완료)** | 카드 풀 10→16종 "재미있는 효과" 방향(§8 참조: heavy_hand·loaded_dice·cold_forge·turning_rack·frostbrand·hobnails) + 모루 lifeblood/warchest | 빌드 재미 |
| **V2.2 (완료)** | 챕터 3 "The White Heart"(조합 기믹 12 레시피 — 어둠+봉인, 점수+어둠, 얼음+안개, 얼음+잔해, 심지+압박 + 최종 보스 3스킬) + 엔드리스 등반(링 맵 무한 연장, 수문장 로테이션, 링 조임, 기록판 endless_best) | 장기 목표 — 끝이 없는 로그라이트 |
| **V2.2a (완료)** | 퓨즈 전역 기믹화 완결: 수련장·래더·자유 듀얼도 순수 보드(퓨즈는 번 룸·캠페인 듀얼 전용), Tempering 모드 제거(드래프트는 캠페인이 흡수), 설정 다이어트(Spins·Line clears·Wall kicks·Fuse rules·Forced drop 슬라이더 제거 — Rules 탭이 Feel 탭이 됨), 점수판 모드 키잉(트레이너 파일은 열람 전용 유산), 착지 셰이크 제거(필드 흔들림은 쿼드·스핀·캐스케이드·크리티컬 등 큰 사건 전용) | 룰은 게임의 것, 눈은 편하게 |
| **V2.2b (완료)** | Flow·오버드라이브를 퓨즈에서 분리(`flow_rail`) — 보상 축은 수련장·래더·캠페인 어디서나 돈다, 퓨즈는 강제 드롭이라는 처벌 축만 담당 + 혼란 카드 4종(CHAOS 가문): 삐뚤어진 재판관·고리 벽·엉킨 배선·느슨한 래칫 + **난이도가 적을 움직인다**(`rank_for`: 순한 불 −1랭크 / 벼린 불 그대로 / 백열 +1랭크, 미니보스·보스·잡몹·레이드 전원) | 보상이 늘 살아 있고, 빌드에 도박수가 생기고, 문 앞의 선택이 진짜 난이도가 됨 |
| **V2.2c (완료)** | 듀얼 사다리 재조정: 챕터마다 스커미시–미니보스–보스가 한 칸씩 오르고(C·B·A → B·A·S → A·S·SS), 각 챕터는 직전 보스보다 한 칸 아래에서 시작한다. 레이드는 무패 3연전이므로 자기 챕터 사다리보다 한 칸 아래(c2r1 C·B·B, c3r1 B·A·A). 최종 보스는 X→SS(블레이드는 그대로 6장). campaigncheck가 "듀얼은 챕터 안에서도, 챕터를 넘어서도 오르기만 한다"를 핀으로 잡는다 | S 보스가 A 한 번 안 겪고 튀어나오던 계단 제거 |
| **V2.3 (완료)** | 수문장 쌍 로테이션: 챕터마다 컨셉 쌍 3조(수문장·망치·재간꾼) 중 시드가 하나를 굴리고, 정체는 한 줄 전에 공개. 신규 듀얼 12장(39→51 레시피). 테이블의 위치 규약을 `Stage::role`/`Stage::pair`와 헬퍼(`chapter_rooms`/`chapter_pairs`/`pair_boss`/`chapter_bosses`)로 교체 — 배틀 윈도가 role 0 집합이라 기존 시드의 맵은 그대로다. 챕터 게이트·엔드리스 해금·수문장 로테이션 모두 "아무 보스"를 역할로 읽는다 | 꼭대기가 매번 같은 얼굴이 아니게 됨 — 재플레이 유인 |
| **V2.4 (완료)** | 카드 풀 20→34종 + 여섯 번째 가문 **WARD(방벽)** 6장(무게추·자유로운 손·바닥 청소·냉담·냉각수·체) + 기존 다섯 가문 8장. 신규 SimConfig 4필드(`free_hold`·`sweep_every`·`garbage_scale`·`flow_ignite`)는 전부 기본값 무해, 신규 `wardcheck` ctest(→28)가 셋을, `fusecheck`가 점화선을 잡는다. 그리고 **후반 물가**: 엠버 상점가가 오른 행마다 +8%, 상한 ×3 (`campaign::priced`) | 드래프트가 매번 달라지고, 후반 엠버가 다시 무거워짐 |
| **V2.5 (완료)** | 듀얼에서 심지 제거 — 캠페인 듀얼만 예외로 타고 있던 것을 끝냈다(`campaign.cpp`의 한 줄). 그 자리에 **보스 스킬 4종 → 10종**(녹비·봉인·냉기·열파·집게·양끝벽·암전·매연·중량·모루 강타)과 진짜 타격감(전용 경고음 + 착탄 플래시 + 화면 흔들림), 신규 `skillcheck` ctest(→29). 좌초한 카드 9장과 모루 2종을 **게이지의 공급** 축으로 재설계(FUEL이 심지가 아니라 Flow를 먹인다), 신규 SimConfig 7필드 전부 무해 기본값. **CHAOS는 백열 전용 챌린지 등급**으로 — 순한 불에서는 한 장도 안 나오고, 백열에서도 한 손에 한 장까지 | 듀얼이 시계가 아니라 상대와 싸우는 것이 됨 |
| **V2.6 (완료)** | 아트 오버홀. 화면의 9할을 차지하는 **블록을 다시 그렸다** — 자기 색의 검은 자리, 4단 그라디언트 면, 모서리를 비켜 가는 **연귀 모따기**, 스페큘러 하나. 우물은 리벳 박힌 도가니 테두리가 되고, 잔해도 같은 주물이 되(식고 금이 간 채) 었다. 생성기(`tools/make_gfx.py`)에 재질 헬퍼(`shaded_poly`/`shaded_ellipse`)를 넣고 **아이콘 21종을 전부 다시 그렸다** — 특히 맵 노드 다섯이 모두 같은 모루였던 버그(지도에서 대장간·미니보스·보스를 눈으로 구분할 수 없었다)를 서로 다른 실루엣으로 해결. 죽은 에셋 4개 삭제, **팔레트를 `cpp/gui/palette.hpp` 한 곳으로** 모으고 갈라져 있던 42개 리터럴을 흡수. **스킬 시전 연출**로 V2.5의 마지막 숙제를 마감 — 처음엔 판때기만 붙였는데 "박진감이 안 느껴진다"는 지적이 맞았다: 판때기는 정적이고, 게다가 **엉뚱한 보드 위에** 떠 있었다. 이제 시전은 세 곳에서 동시에 일어난다 — 적의 우물이 스킬 색으로 달아오르고 티끌이 중심으로 빨려들며 마지막 1초에 보드 자체가 떨리고(`cast_kick`), 판때기가 그 우물에 붙거나(가로) 테더로 연결되고(세로 — 읽을 만한 폭의 판때기는 폰에서 내 스택을 가려 버린다), **와인드업 마지막 20프레임 동안 볼트 3발이 적 보드에서 내 보드로 날아와 규칙상 효과가 터지는 바로 그 프레임에 도착한다.** 적은 반동으로 밀려나고, 내 우물엔 충격파 링과 파편이 튀고, 화면은 3프레임만 세게 물들었다 걷힌다. 신규 큐 `skillcast`(38종). 안드로이드 **재설치 시 옛 그림이 남던 버그** 수정(에셋 목록이 크기를 싣고, 크기가 다르면 덮어쓴다) | 그림이 처음으로 게임과 같은 급이 됨 |
| **V2.7 (완료)** | 봇 사다리 재조정 — 순한 불이 진짜 초보용이 됐다. 리그 아래 두 칸 **F(0.45 PPS)·E(0.58)** 신설(D는 TL 실제 평균이라 입문자보다 빠르다), 순한 불은 **두 칸 아래 + 상한 B**(`kMildCeiling`). 순한 불 사다리가 챕터1 E→D→C, 챕터2 D→C→B, 챕터3 C→B→B로 전부 E~B 구간에 들어왔다 — 예전엔 순한 불에서도 길의 끝이 S였다. **벼린 불·백열은 단 한 칸도 안 움직였다** (레시피 등급을 +2 하고 이동식을 그만큼 조정) | 입문자가 캠페인을 끝까지 볼 수 있게 됨 |
| **V2.8 (완료)** | 런 성적. 라운드마다 뜨던 TETR.IO TR 추정치를 **런 안에서는 걷어냈다** — 한 판을 공개 평균에 대보는 숫자라, 결과가 나오지도 않은 등반 도중에 열두 번 나올 이유가 없다(수련장에선 그게 그 모드의 존재 이유라 그대로). 대신 등반이 끝나면 **THE CLIMB 등급**: 진행도 55 + 목숨 25(무사고 만점, 사망당 −8) + 시간 20(행당 150초 par) → 불 배율(×0.85/1.0/1.15) → S/A/B/C/D. 등급 밑에 그걸 만든 세 숫자(행·사망·시간)를 같이 찍어서 다음 런에 뭘 고칠지가 보이게 했다. `Run`에 `seconds`·`deaths` 추가(**전투 누적 시간**이지 벽시계가 아니다 — 켜 놓고 잔 런이 나쁜 런은 아니다), campaigncheck 핀 9개 | 등반이 처음으로 하나의 결과를 갖게 됨 |
| **V2.9 (완료)** | 읽히는 게임 만들기 두 가지. **적 이름 옆 랭크 문자 제거** — `Bot (B)`가 아니라 `The Keen Underwarden`. 등급마다 형용사 하나(`bot::might_of`: Half-forged/Green/Rough/Tempered/Keen/Honed/Master/Grandmaster/Peerless/Godmetal)를 이름 앞에 붙이고, 문자는 고르는 버튼 줄에만 남긴다(고를 땐 짧은 손잡이가 필요하니까) — 대신 버튼 밑에 고른 등급을 말과 속도로 적는다. 그리고 **스테이지 설명 분리**: 승리 조건은 레시피에서 계산한 `campaign::goal_line`이 평이한 한 문장으로 말하고("Clear 15 lines."), blurb은 그 방이 뭐가 다른지만 말한다. 예전엔 한 문장에 섞여 있어서("Old iron on the floor. Fifteen lines through it.") 초보가 어느 쪽이 규칙인지 골라내야 했고, 손으로 적은 숫자가 실제 규칙과 어긋날 수도 있었다 — 계산된 줄은 어긋날 수 없고 핀이 그걸 잡는다. 51개 blurb 전면 재작성 | 처음 보는 사람이 노드를 읽고 뭘 할지 안다 |
| **V3.0 (완료)** | **레이드가 1대3 동시전이 됐다.** 예전엔 라운드마다 한 명씩 붙는 관문(gauntlet)이었는데, 화면에 보드 셋이 있는 건 어디서도 그런 뜻이 아니다 — 테트리오 멀티처럼 셋이 동시에 플레이하고 **셋 다 나한테 보낸다**. 내 공격은 **한 명에게만** 간다: 누굴 먼저 묻을지(터지기 직전인 놈이냐, 제일 세게 때리는 놈이냐)가 이 모드의 유일한 판단이고, 그래서 조준은 플레이어가 옮긴다 — 키보드는 Tab, 폰은 **묻고 싶은 보드를 터치**. 조준된 적은 엠버 테두리를 두르고 내 우물 위 슬롯이 이름을 말한다. 적이 터져도 라운드가 안 끝난다 — 그 보드만 식은 채로 멈추고 와이어에서 빠지며, 마지막 하나가 쓰러지면 방이 정리된다. `VersusMatch`가 `std::vector<std::unique_ptr<Foe>>`로 바뀌고 각 적이 자기 시드를 받는다(같은 시드면 한 적을 셋 그린 것). 쓰레기가 세 갈래로 들어오므로 로스터 등급을 낮췄다(c2r1 `1,1,2` / c3r1 `2,2,3`) — campaigncheck가 "방 안의 모든 적은 혼자 만나는 적보다 약하다"를 핀으로 잡는다. skillcheck에 방 검사 6개 | 3대1이 쓰레기 세 배가 아니라 전략이 됨 |
| **V3.0a (완료)** | **사건마다 고유한 모양.** 겉모습은 계속 손봤는데 게임이 여전히 밋밋했던 이유가 코드에 있었다 — 열한 개 큐가 전부 같은 `spawn_sparks`를 부르고 **색과 개수만 달랐다**. 이펙트가 하나였던 것이지 열한 개가 아니었다. 그래서 어휘부터 늘렸다: 기존 **Spark**(둥근 잔광)에 **Shard**(각진 파편 — `SDL_RenderGeometry` 사각형이 회전하며 스파크의 두 배 중력으로 떨어진다), **Ring**(퍼져 나가며 얇아지는 파동), **Beam**(우물을 세로로 관통하는 빛기둥 — 식으면서 가운데로 좁아진다)을 더했다. 그리고 큰 사건마다 자기 모양을 줬다 — **얼음 파쇄**(유저가 든 예): 심은 얼 때만 `freeze`를 말하고 깨질 때는 아무 말도 안 해서 평범한 클리어처럼 보였다. 화면이 `iron_row()` 마스크를 프레임마다 비교해 **얼음이던 줄이 얼음이 아니게 되는 순간**을 잡아 창백한 파편을 열 칸 전부에서 터뜨리고, 줄 전체 길이의 흰 균열을 긋고(번지는 화상이 아니라 이미 다 갈라진 금), 짧게 딱 끊는 진동을 준다. **쿼드**는 우물을 관통하는 금빛 빛기둥 + 네 줄 전 칸에서 튀는 파편(2줄·3줄은 그보다 적게 — 이펙트 크기가 곧 클리어의 값어치다). **티스핀**은 링이 열리고 파편이 **원의 접선 방향**으로 날아간다(돈 방향이 보인다). **퍼펙트 클리어**는 우물 전체가 하얘지고 파동이 나간다. **쓰레기 상승**은 `pending_garbage()`가 줄어드는 프레임을 잡아 바닥에서 먼지가 튀어 오른다 — 예전엔 스택 밑으로 소리 없이 밀려 올라왔다. **심은 한 줄도 안 건드렸다** — 전부 공개 접근자를 화면이 읽어서 만든 것이라 채점·트레이스 등가성은 그대로다 | 같은 플레이가 처음으로 사건처럼 보임 |
| **V3.1a (완료)** | 플레이 피드백 일곱 개를 한 아크로. **(1) 상대 보드도 같은 블록** — 적 우물은 아직 납작한 색 사각형이었다(`draw_cell`이 아니라 `fill`). 대칭 듀얼에서 같은 크기로 나란히 놓고 한쪽만 질감이 없으면 다른 게임 둘로 보인다. **(2) 입력이 씹히던 진짜 버그** — 게이트가 press와 release를 **똑같이** 막고 있었다. ImGui가 한 프레임 키보드를 가져가거나, 카운트다운이 시작되거나, 패널이 열리는 순간이 키를 누른 뒤 떼기 전에 오면 **release가 삼켜지고 심은 그 키를 계속 눌린 것으로 안다** — 다음 입력이 no-op이 되니 "랙처럼 키가 씹히는" 증상이 된다. 이제 release는 어떤 게이트도 막지 않고(뗀 손을 막아서 좋을 일은 없다), 창이 포커스를 잃으면 모든 키를 놓는다(알트탭 후 피스가 혼자 달리던 것). **(3) CHAOS는 카드를 떠나 저주가 됐다** — 두 버전 동안 아무도 안 골랐다. 손을 망가뜨리는 카드는 고를 만하게 보상을 붙여야 하는데, 그러면 득실이 상쇄돼 아무 의미도 없는 카드가 된다. 그래서 보상을 걷어내고 선택권도 걷어냈다: **엔드리스가 두 링마다 하나씩 강제로 얹는다**(순서는 런 시드, 다섯 개면 끝). 저주는 엠버로 태울 수 있지만 값이 카드 제거의 세 배다. **(4) 삐뚤어진 재판관 = 개사기 수정** — "양옆이 막힌 착지는 전부 풀스핀"이었는데, 실제 스택에서 그건 착지 대부분이다: 절대 안 끊기는 B2B 사슬, 게임에서 제일 센 효과. 이름이 늘 말하던 쪽으로 뒤집었다 — **어떤 스핀도 점수가 안 난다**. **(5)(6) 엔드리스가 진짜로 조여진다** — `endless_rank`의 상한이 index 7에 박혀 있어서 10칸 사다리의 위 두 칸이 도달 불가였고, 링 8쯤부터 적이 성장을 멈췄다(플레이어 빌드는 노드마다 무한히 자라는데). 상한을 사다리 끝으로 올리고, **그 위로는 못 준 승급을 공격력으로 지급**(`endless_edge`), 그리고 **플레이어 보드에 떨어지는 쓰레기 무게가 링마다 무한히 증가**(`endless_press`). 목표선·중력·홍수 주기도 전부 한 단계씩 더 조였다. **(7) 엠버 물가** — 수급 `lines*2 + attack*3` → `lines + attack`, 가격 리롤 6→8·추가픽 14→22·제거 8→14. 40줄 60공격 방이 260을 벌어 65짜리 상점을 통째로 사고도 남던 것이 이제 100을 번다 | 컨트롤이 씹히지 않고, 엔드리스가 끝없이 어려워지고, 엠버가 다시 선택이 됨 |
| **V3.1b (완료)** | **레이드 방이 초점 레이아웃이 됐다.** 적 보드 셋을 *한 보드 폭*에 욱여넣고 있어서 셀이 4픽셀이었고, 그 아래 화면 절반이 통째로 비어 있었다 — 읽히지도 않고 실수처럼 보였는데 실제로 실수였다. 이제 방은 플레이어 오른쪽 **띠 전체**를 쓰고(실제 창 크기에서 재므로 폰에서도 안 넘친다), 셋을 균등하게 나누지 않는다: **조준한 보드가 크고 나머지는 물러난다.** 조준을 옮기면 크기가 이동한다(프레임마다 lerp) — 이 모드의 유일한 판단이 조준이니, 전환이 기록이 아니라 사건으로 보여야 한다. 쓰러진 보드는 더 줄어들고, 셋 다 **한 바닥선에 정렬**돼 높이가 다른 게 어긋난 줄이 아니라 방으로 읽힌다. 보드마다 **자기 Flow 레일과 들어오는 쓰레기 계기**를 갖는다(예전엔 고정 오프셋에 하나만 그려서 옆 보드 위에 얹혔다). 점수판은 방 옆이 아니라 **아래로** 내려가고, 방 높이는 그 점수판이 창 밖으로 밀리지 않도록 마지막에 잘린다. **그리고 스킬 딜이 난이도를 읽는다** — 레시피가 숫자를 쓰고, 문 앞에서 고른 불이 그 숫자의 값을 정한다(`campaign::skill_scale`): 순한 불 ×0.6, 벼린 불 ×1.0(레시피 그대로), 백열 ×1.4, 엔드리스는 1.4에서 링마다 +0.08로 **상한 없이**. 던지는 줄 수·빨아가는 게이지·기믹을 눌러 두는 시간 전부에 걸린다 — 녹비가 순한 불 2줄, 벼린 불 3줄, 백열 4줄. 바닥이 있어서(최소 1줄, 최소 1초) 2초 동안 지켜본 일격이 아무것도 아닌 일은 없다 | 3대1이 화면에서 읽히고, 불의 선택이 맞는 매의 무게까지 바꿈 |
| **V3.2 폴리싱 (완료)** | 사이사이가 비어 있던 것들. **화면 전환** — 47군데가 화면을 바꾸는데 전환을 각자 기억해야 했다면 절반은 없었을 것이므로, 아무도 알리지 않고 **커튼이 변화를 감시한다**(`screen_was`와 비교). 그을음 장막이 뜨거운 이음선을 남기며 걷힌다 — 눈이 밝은 선 하나를 따라 올라가지, 사각형이 덜 회색이 되는 걸 보지 않는다. 스모크 한 판의 화면 전환 38번 전부에서 뜨는 걸 확인했다. **게임 시작** — 카운트다운 뒤에서 도가니가 바닥부터 달아오른다(그을음이 얇아지고 바닥 불빛이 우물을 타고 오른다). 첫 피스가 늘 켜져 있던 곳이 아니라 **준비된 곳**으로 떨어진다. **게임 종료** — 보드가 그냥 멈추는 대신 반 초 동안 식는다. 보드는 계속 그려 둔다(사라진 우물 위의 결과창은 크래시로 읽힌다). **그리고 런 시작의 망치** — 등반이 시작될 때 대장 망치가 오른쪽 위에서 내려와 **트리의 발치**(런이 실제로 시작되는 맨 아랫줄)를 내려치고, 충격이 지도를 타고 올라가며 길을 밝힌다. 새 에셋 `gfx/maul.png`(생성기에서, 세 번 고쳐 그렸다 — 넓고 얇은 머리는 표지판, 발치로 옮기면 받침대로 읽혔다; 한쪽은 납작한 타격면, 반대쪽은 뾰족한 펜이라는 **비대칭**이 도구로 만든다). 망치가 떠 있는 동안 노드는 **선택 불가**다 — 착탄 전에 클릭해 넘길 수 있으면 타격도 클릭도 없는 셈이라서 — 그리고 맵은 그동안 발치로 스크롤한다 | 게임이 시작하고 끝나는 게 보임, 등반의 첫 프레임이 사건이 됨 |
| **V3.3 열린 지도 (완료)** | **맵이 하나의 결말로 깔때기처럼 모이던 것을 끝냈다.** 맨 윗줄이 노드 **하나**였고(`widths[kMapDepth-1] = 1`) 지도의 모든 길이 거기로 빨려들었다 — 그게 폐쇄적으로 느껴진 진짜 이유다. 이제 챕터마다 **서로 다른 컨셉 쌍에서 온 감시자 2~3명**이 꼭대기에 서고, 어떤 결말을 보는지는 시드가 아니라 **걸어온 길**이 정한다. 가운데도 넓혔다: 입구 2~3, 중간 3~4(예전엔 2~3 — 줄마다 선택지 하나면 그건 배 나온 복도지 지도가 아니다). campaigncheck가 "중간은 절대 입구보다 좁지 않다", "모든 결말은 서로 다른 감시자다", "미니보스는 그중 한 쪽에 속한다"를 핀으로 잡는다. Career 패널은 480→680으로 넓혔다. **그리고 망치가 칠 것이 생겼다** — 새 에셋 `gfx/stithy.png`(뿔·굽·허리·발이 있는 제대로 된 모루)가 트리 발치에 서고 망치가 그 작업면을 때린다. 착탄 후 망치는 사라지지 않고 **모루 위에 얹혔다가 들려 나간다**(치는 프레임에 사라지는 건 글리치로 읽힌다). **화면 흔들림은 실은 아예 안 걸리고 있었다** — 보드 자체의 흔들림은 게임 화면에서만, 보드 판만 흔든다. 맵에서 친 타격은 아무것도 못 흔들었다. 신규 `App::jolt`이 **ImGui 패스 전체의 뷰포트를 밀어서** 화면 전부를 흔든다(게임 중 최대 세기, 22프레임). **그리고 용암** — 트리는 평평한 판때기 위에 서 있었고 그래서 화면이 도표로 읽혔다. 이제 노드 **뒤로**(ImGui 채널 분리) 발치의 용탕과 레인 사이를 타고 오르는 광맥이 흐르고, 망치의 충격이 올라가는 동안 뜨겁게 타다 잉걸로 가라앉는다 | 지도가 복도가 아니라 갈림길이 됨, 챕터마다 결말 셋 |
| **V3.1** | 배포 마감: 첫 실행 온보딩 순화, 저장 안정성 감사, (선택) 한국어 로컬라이즈, itch/APK 패키징 | 남에게 건넬 수 있는 물건 |

버전 하나 = 아크 하나 = 커밋·매트릭스·배포물 한 세트. 순서는 바꿀 수
있어도 건너뛰지 않는다 — 특히 V2.0-b 없이 V2.0-c의 기믹부터 만들지 않는다
(사건은 맵 위에서만 의미가 있다).

## 10. 불변 제약 — 어기면 안 되는 것들

1. **심의 20ms/50Hz 그리드**와 파이썬 트레이스 등가성 그레이딩. 표시는
   보간해도(Smooth motion) 심은 절대 만지지 않는다.
2. **저장 id 동결**: 스테이지 id, 모루 id, 카드 id, 그리고 앞으로 생길
   노드/기믹/난이도 id. 한 번 파일에 쓰인 id는 영원하다.
3. **저장 형식은 관용 key=value**: 모르는 줄은 보존하고 되쓴다. 구버전이
   신버전 세이브를 먹으면 안 된다.
4. **모루는 캠페인 한정** — 일반 모드 점수표의 순수성.
5. **핸들링 기본값은 유저 소유** — 밸런스 문제를 핸들링으로 풀지 않는다.
6. **새 심 기능 = 새 ctest**: 기믹 하나가 심에 들어오면 그 기믹의
   결정론적 검사가 같이 들어온다.
7. **모든 새 화면은 폰 세로 스크린샷 검증**을 거친다.
8. **비주얼은 `gfx/` 에셋 + `tools/make_gfx.py`가 원본**: 그림을 고치려면
   스크립트를 고치고 재생성한다. 모든 그리기는 에셋 부재 시 절차 폴백을
   가진다.
9. **색은 두 곳에만 산다** (V2.6): `cpp/gui/palette.hpp`(라이브로 그리는
   쪽)와 `tools/make_gfx.py` 머리의 상수(PNG에 굽는 쪽). 둘은 같은 숫자를
   들고 있는 한 쌍이므로 한쪽을 고치면 다른 쪽도 고치고 생성기를 다시
   돌린다. 새 리터럴을 그리기 코드에 직접 박지 않는다 — 그렇게 다섯 갈래로
   갈라져서 엠버가 여덟 가지 주황이 되어 있었다.

## 11. 런 상태 저장 키 (V2.0-b 예약)

`campaign.dat`의 관용 key=value에 아래 키를 예약한다. V2.0-a에서는 쓰지
않지만, 여기 명세해 두어 구현 아크가 이름을 새로 짓지 않게 한다.

```
difficulty mild|forged|white   # 진행 중인 런의 난이도
run_chapter <int>              # 오르고 있는 챕터
run_seed <unsigned>            # 맵 그래프의 시드 (같은 시드 = 같은 맵)
run_depth <int>                # 현재 깊이 (0 = 입구, path 길이에 묶임)
run_path <i,i,...>             # 깊이별로 고른 노드 인덱스
run_tempers <id,id,...>        # 런의 템퍼 스택 (순서 보존)
run_embers <int>               # 런의 엠버 잔액
run_lives <int>                # 벼린 불의 남은 목숨
```

- **`run_seed`의 존재가 곧 "진행 중인 런"이다** (V2.0-b 구현 확정).
  런이 끝나면 키를 쓰지 않는 것으로 소거한다 — 별도의 `run_over`
  마커는 필요 없었다. 기존 진행(`stage <id> <stars>`)과 슬래그·모루
  키는 그대로 공존한다 — 별은 노드 클리어로도 쌓인다.
