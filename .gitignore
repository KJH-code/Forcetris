__pycache__/
*.py[cod]

# Save data written next to the game at runtime: scores, and the player's profile.
data/*.dat
data/*.json
data/replays/
data/back/*.bak

# Crash dumps and screenshots.
crashdump.log
screenshots/*.png

# cx_Freeze output, and the C++ build trees (native and cross).
build/
dist/
cpp/build*/
reference.txt
